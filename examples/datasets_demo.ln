import datasets

var data = datasets::load_csv("../examples/iris_dummy.csv")
display "rows:"
display len(data)

var sepal_lengths = datasets::get_column(data, "sepal_length")
display "sepal_length sample:"
display sepal_lengths

display "dataset demo complete"
